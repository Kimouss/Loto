{% extends 'base.html.twig' %}

{% block title %}Draw index{% endblock %}

{% block body %}
    <h1>Draw index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'NbDraw', 'draw.nbDraw') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Day', 'draw.day') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Date', 'draw.date') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Ball1', 'draw.ball1') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Ball2', 'draw.ball2') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Ball3', 'draw.ball3') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Ball4', 'draw.ball4') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Ball5', 'draw.ball5') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'LuckyBall', 'draw.luckyBall') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'WinComboAsc', 'draw.winComboAsc') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'actions', '') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for draw in pagination %}
            <tr>
                <td>{{ draw.nbDraw }}</td>
                <td>{{ draw.day }}</td>
                <td>{{ draw.date ? draw.date|date('Y-m-d') : '' }}</td>
                <td>{{ draw.ball1 }}</td>
                <td>{{ draw.ball2 }}</td>
                <td>{{ draw.ball3 }}</td>
                <td>{{ draw.ball4 }}</td>
                <td>{{ draw.ball5 }}</td>
                <td>{{ draw.luckyBall }}</td>
                <td>{{ draw.winComboAsc }}</td>
                <td>
                    <a href="{{ path('app_draw_show', {'id': draw.id}) }}">show</a>
                    <a href="{{ path('app_draw_edit', {'id': draw.id}) }}">edit</a>
                </td>
            </tr>

        {% else %}
            <tr>
                <td colspan="30">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <a href="{{ path('app_draw_new') }}">Create new</a>
{% endblock %}
