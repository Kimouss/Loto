{% set current_path = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_dashboard') }}">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if current_path == 'app_dashboard' %}active{% endif %}" {% if current_path == 'app_dashboard' %}aria-current="page{% endif %}" href="{{ path('app_dashboard') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if current_path == 'app_draw_index' %}active{% endif %}" {% if current_path == 'app_draw_index' %}aria-current="page{% endif %}" href="{{ path('app_draw_index') }}">Search</a>
                </li>
            </ul>
            <form class="d-flex">
                {{ include('partials/_switch_theme.html.twig') }}
            </form>
        </div>
    </div>
</nav>
